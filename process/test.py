import torch
import numpy as np
# import copy
# k = 3

# a_ = []
# for n in range(k):
#     b = a.view(-1)
#     a_.append(b)
#
# c = torch.stack(a_, -1).view(-1)
# print(c)

list = [[0.111, 0.555], [0.123, 0.666], [0.666, 0.777]]
print(np.array(list))